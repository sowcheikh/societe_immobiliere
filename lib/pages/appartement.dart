import 'package:flutter/material.dart';

class AppartementRoute extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Appartement'),
      ),
      body: Column(
        children: [
          // Insérez vos images et descriptions ici
        ],
      ),
    );
  }
}