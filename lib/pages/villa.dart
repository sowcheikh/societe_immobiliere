import 'package:flutter/material.dart';

class VillaRoute extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Villa'),
      ),
      body: Column(
        children: [
          // Insérez vos images et descriptions ici
        ],
      ),
    );
  }
}